# Tickets / MVP-Backlog

Der ausführliche Ticketplan liegt als Excel-Datei unter `docs/` bzw. im Projektpaket.

Empfohlene Umsetzung in GitHub Issues:

1. Epics als GitHub Issues anlegen.
2. Einzelne MVP-Tickets aus `MVP_Ticketplan_JA_ClaimsAssist_v0.2.xlsx` übernehmen.
3. Labels verwenden: `frontend`, `backend`, `database`, `ki`, `mvp`, `security`.
4. Issues mit Meilenstein `MVP v0.1` versehen.
